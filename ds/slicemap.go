package ds

// // Acts like a map, but backed by a slice instead of a map
// type SliceMap[V any] struct {
// 	slice []V
// }

// func(m *SliceMap[V]) Put(idx int, val V) {
// 	// TODO: Bounds check and grow

// 	m.slice[idx] = val
// }

// func(m *SliceMap[V]) Get(idx int) (V, bool) {
// 	// TODO: Bounds check and exit early

// 	return m.slice[idx], true
// }

// func(m *SliceMap[V]) Delete(idx int) {

// }

// func(m *SliceMap[V]) Clear() {

// }
